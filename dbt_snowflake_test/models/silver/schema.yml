version: 2

models:
  - name: orders
    columns:
      - name: order_date
        tests:
          - not_null
      - name: total_price
        tests:
          - no_negative_values

sources:
  - name: bronze_source      # This is the source name (used in `source('public', ...)`)
    database: jr_lab_db1
    schema: bronze    # This is the actual Snowflake schema name
    tables:
      - name: nations_raw
      - name: regions_raw
      - name: orders_raw
      - name: customers_raw